while getopts "b:" o; do
    case "select file in *.*
do
  echo file
done" in
        b)
            basedir="OPTARG"
            ;;
        *)
            usage
            ;;
    esac
done
shift $((OPTIND-1))


if [[ -z "basedir" ]]; then
    printred "parameter -b not defined.\n"
    usage
fi
